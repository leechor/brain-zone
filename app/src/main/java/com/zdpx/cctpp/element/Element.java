package com.zdpx.cctpp.element;

/**
 * Elements hold states for an object instance.
 * <br>
 * Elements define the dynamic components of an object and may have both properties(input parameters) and states
 * (runtime changeable values). within an object the tokens may execute steps that change the states of the elements
 * that are owned by the object.
 * <p>
 * Elements represent things in a process that change state over time.
 * Elements are added to a list and then referenced by one or more steps in a process.
 * </p>
 */
public class Element {

}
