package com.zdpx.cctpp.enu;

/**
 *The wait action to be taken by the token arriving to the Transfer step. Options include
 * WaitUntilTransferred and WaitUntilTransferring.
 */
public enum TokenWaitAction {
    WAIT_UNTIL_TRANSFERRED,
    WAIT_UNTIL_TRANSFERRING
}
